<template>
  <span>{{ text }}</span>
</template>

<script>
export default {
  /*----------------------------------------------------------------------------------------------------
   * 如果 props 定义成一个 object，则它的 key 表示 prop name，它的 value 表示 prop type，
   * prop type 是一个类/构造函数，
   * 当传递的 props 类型不匹配时，会在浏览器控制台打印警告信息，
   *----------------------------------------------------------------------------------------------------*/
  props: {
    text: String,
  },
};
</script>
